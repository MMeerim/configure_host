---
- name: User Add
  become: yes
  vars:
    users:
    - username: "axs176"
    - username: "hde927"
    - username: "xso366"
    - username: "yli204"
    - username: "kjp672"
  tasks:
  - name: Adding User "{{ item.username }}"
    user:
      name: "{{ item.username }}"
      create_home: yes
      groups: wheel
    with_items: "{{ users }}"